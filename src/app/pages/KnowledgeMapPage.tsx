import { useEffect, useMemo, useState } from 'react'
import { Link, useSearchParams } from 'react-router-dom'
import { contentRepository } from '../../lib/content-client'
import { buildKnowledgeMapViewModel, mapFilters, type KnowledgeMapViewModel } from '../data/knowledge-map-model'
import { useLocalStateSnapshot } from '../state/use-local-state'

export function KnowledgeMapPage() {
  const [view, setView] = useState<KnowledgeMapViewModel>(); const [error, setError] = useState<string>(); const [params, setParams] = useSearchParams(); const [relations, setRelations] = useState(true)
  const local = useLocalStateSnapshot()
  useEffect(() => { const controller = new AbortController(); void Promise.all([contentRepository.loadKnowledgeMap(controller.signal), contentRepository.loadCatalog(controller.signal), contentRepository.loadTaxonomy(controller.signal), contentRepository.loadRoutesIndex(controller.signal)]).then(([map, catalog, taxonomy, routes]) => setView(buildKnowledgeMapViewModel(map, catalog, taxonomy, routes))).catch((reason: unknown) => { if (!(reason instanceof DOMException && reason.name === 'AbortError')) setError(reason instanceof Error ? reason.message : '知识地图无法加载') }); return () => controller.abort() }, [])
  const filters = useMemo(() => view ? mapFilters(params, view) : { domain: '', course: '', route: '', node: '' }, [params, view])
  const setFilter = (next: Partial<typeof filters>) => { const value = { ...filters, ...next }; const query = new URLSearchParams(); for (const key of ['domain', 'course', 'route', 'node'] as const) if (value[key]) query.set(key, value[key]); setParams(query) }
  const nodes = useMemo(() => view?.nodes.filter((node) => (!filters.domain || node.domainId === filters.domain) && (!filters.course || node.courseId === filters.course)) ?? [], [filters, view])
  const states = useMemo(() => new Map(local.nodeStates.map((state) => [state.node_id, state])), [local.nodeStates])
  if (error) return <section className="atlas-page"><h1 tabIndex={-1}>知识地图</h1><p role="alert">{error}</p></section>
  if (!view) return <section className="atlas-page"><h1 tabIndex={-1}>知识地图</h1><p role="status">正在加载……</p></section>
  const focused = view.nodes.find((node) => node.id === filters.node)
  const courses = filters.domain ? view.domains.find((entry) => entry.id === filters.domain)?.courses ?? [] : view.domains.flatMap((entry) => entry.courses)
  const related = focused ? view.edges.filter((edge) => edge.from === focused.id || edge.to === focused.id) : []
  const selectedRoute = view.routes.find((route) => route.id === filters.route)
  const nodeHref = (id: string) => { const query = new URLSearchParams({ map: '1' }); for (const key of ['domain', 'course', 'route', 'node'] as const) if (filters[key]) query.set(key, key === 'node' ? id : filters[key]); return `/node/${id}?${query}` }
  return <section className="atlas-page knowledge-map-page"><p className="atlas-coordinate">MAP / KNOWLEDGE</p><h1 tabIndex={-1}>知识航图</h1><div className="knowledge-map-controls"><label>领域<select value={filters.domain} onChange={(event) => setFilter({ domain: event.target.value, course: '', node: '' })}><option value="">全部领域</option>{view.domains.map((entry) => <option key={entry.id} value={entry.id}>{entry.name}</option>)}</select></label><label>课程<select value={filters.course} onChange={(event) => setFilter({ course: event.target.value, node: '' })}><option value="">全部课程</option>{courses.map((entry) => <option key={entry.id} value={entry.id}>{entry.name}</option>)}</select></label><label>路线叠层<select value={filters.route} onChange={(event) => setFilter({ route: event.target.value })}><option value="">不显示</option>{view.routes.map((entry) => <option key={entry.id} value={entry.id}>{entry.code} · {entry.name}</option>)}</select></label><label className="knowledge-map-toggle"><input type="checkbox" checked={relations} onChange={(event) => setRelations(event.target.checked)} />显示关系</label></div>{selectedRoute && <p className="knowledge-map-route" data-route={selectedRoute.id}>路线：{selectedRoute.code} · {selectedRoute.name}｜{selectedRoute.summary}</p>}{nodes.length === 0 ? <div className="atlas-empty"><span>00</span><p>{view.nodes.length ? '当前筛选下没有节点。' : '知识地图还没有节点。加入正式知识后，这里会按领域和课程形成航图。'}</p></div> : <div className="knowledge-map-tracks">{view.domains.filter((domain) => !filters.domain || domain.id === filters.domain).map((domain) => { const domainNodes = nodes.filter((node) => node.domainId === domain.id); if (!domainNodes.length) return null; return <section key={domain.id} className="knowledge-map-domain"><h2>{domain.name}</h2>{domain.courses.filter((course) => !filters.course || course.id === filters.course).map((course) => { const track = domainNodes.filter((node) => node.courseId === course.id); if (!track.length) return null; return <section key={course.id} className="knowledge-map-course"><h3>{course.name}</h3><ol>{track.map((node) => { const state = states.get(node.id); const inRoute = selectedRoute?.nodeIds.includes(node.id); return <li key={node.id} className={inRoute ? 'map-node map-node-route' : 'map-node'} data-focused={node.id === filters.node}><span>{String(node.sequence).padStart(2, '0')}</span><button type="button" onClick={() => setFilter({ node: node.id })}><strong>{node.title}</strong><small>{node.courseName} / {node.sequence}{state?.completed ? ' · 已完成' : state?.favorite ? ' · 已收藏' : state?.unknown ? ' · 不会' : ''}</small><p>{node.summary}</p></button></li> })}</ol></section> })}</section> })}</div>}{focused && <aside className="knowledge-map-focus" aria-live="polite"><p className="atlas-coordinate">FOCUS / {focused.id}</p><h2>{focused.title}</h2><p>{focused.domainName} / {focused.courseName} · 第 {focused.sequence} 节</p><p>{focused.summary}</p>{relations && <ul>{related.map((edge) => <li key={`${edge.type}-${edge.from}-${edge.to}`}>{edge.type === 'prerequisite' ? '前置关系' : edge.type === 'related' ? '关联关系' : '路线关系'}：{edge.from === focused.id ? edge.to : edge.from}</li>)}</ul>}<Link to={nodeHref(focused.id)}>打开知识</Link></aside>}<p className="knowledge-map-meta">版本 {view.contentVersion} · {view.edges.length} 条关系</p></section>
}
